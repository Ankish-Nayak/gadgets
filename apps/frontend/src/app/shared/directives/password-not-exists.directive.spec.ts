import { PasswordNotExistsDirective } from './password-not-exists.directive';

describe('PasswordNotExistsDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordNotExistsDirective();
    expect(directive).toBeTruthy();
  });
});
