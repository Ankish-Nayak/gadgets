import { EmailNotExistsDirective } from './email-not-exists.directive';

describe('EmailNotExistsDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailNotExistsDirective();
    expect(directive).toBeTruthy();
  });
});
